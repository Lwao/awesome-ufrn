/* Seja a[] um vetor qualquer, independente de tipo e tamanho, 
e pa um ponteiro para o mesmo tipo de a[]. Verifique se as 
afirmações a seguir são verdadeiras ou falsas:


( V ) Após a atribuição pa=a[0], pa e a possuem valores idênticos;
( V ) A atribuição pa=&a[0] pode ser escrita como pa=a;
( F ) Uma referência a a[i] pode ser escrita como &(a+i);
( V ) &a[i] e a+i são idênticos;
( V ) a+i é o endereço do i-ésimo elemento do vetor;
( V ) pa[i] é idêntico a *(pa+i);
( V ) pa=a é uma operação válida;
( V ) pa++ é uma operação válida;
( F ) a=pa é uma operação válida;
( V ) a++ é uma operação válida.
